{
  "name": "personal-portflolio",
  "version": "1.0.0",
  "description": "A Node js based web application which serves as a dynamic showcase of my professional work.",
  "main": "index.js",
  "scripts": {
    
      "predeploy": "npm run build",
      "deploy": "gh-pages -d build",
  
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.1.1"
  }
}
